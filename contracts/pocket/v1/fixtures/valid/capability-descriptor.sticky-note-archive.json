{
  "$schema": "hoverpocket://schemas/capability-descriptor/v1",
  "id": "sticky.note.archive",
  "version": 1,
  "titleKey": "capability.sticky.note.archive",
  "effect": "reversible_local_write",
  "permissions": ["sticky.write"],
  "approvalPolicy": "broker_policy",
  "idempotency": "required",
  "availability": {
    "macos": "available",
    "windows": "available"
  },
  "limits": {
    "timeoutMs": 3000,
    "maxPayloadBytes": 4096,
    "maxCallsPerMinute": 120
  },
  "inputSchema": {
    "type": "object",
    "required": ["noteId"],
    "properties": {
      "noteId": {
        "type": "string",
        "format": "uuid"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "required": ["noteId", "state", "updatedAt"],
    "properties": {
      "noteId": {
        "type": "string",
        "format": "uuid"
      },
      "state": {
        "const": "archived"
      },
      "updatedAt": {
        "type": "string",
        "format": "date-time"
      }
    },
    "additionalProperties": false
  },
  "readback": {
    "strategy": "capability_query",
    "capabilityId": "sticky.note.status",
    "capabilityVersion": 1,
    "match": ["noteId", "state", "updatedAt"]
  }
}
